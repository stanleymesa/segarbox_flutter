// Main AppBar
const double headerHeight = 90;
const double appBarHeight = 85;

// Rounded Stack Container
const double stackHeight = 20;
const double defaultStackTop = expandedHeight - stackHeight;

// Profile Image
const double expandedHeight = 250;
const double diameter = 80;
const double defaultTop = expandedHeight - (diameter / 2);
